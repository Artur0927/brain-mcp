# Sample vault

Layout used by `deploy.sh` when `./vault` is missing.

```bash
cp -r examples/sample-vault ./vault
export BRAIN_VAULT=./vault
brain-index
```

MCP client: [docs/setup.md](../../docs/setup.md).
